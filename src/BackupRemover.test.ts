
test("doesnt remove fresh backups", () => {
    expect(3).toBe(3);
});


test("removes old backups", () => {
    expect(3).toBe(3);
});

test("removes doble copies of a day ", () => {
    expect(3).toBe(3);
});

test("consoles that dates are not correct", () => {
    expect(3).toBe(3);
});

test("consoles if cant delete file", () => {
    expect(3).toBe(3);
});

test("consoles if cant delete file", () => {
    expect(3).toBe(3);
});
